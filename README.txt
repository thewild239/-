# Бинаризация Оцу + Морфологическое закрытие дыр

## Описание

Приложение для автоматической бинаризации изображений методом Оцу с последующим
морфологическим закрытием для заполнения чёрных пустот внутри объектов.

## Функциональность

- Загрузка изображения (поддерживаются JPG, PNG, BMP, TIFF).
- Автоматическое вычисление порога бинаризации методом Оцу.
- Морфологическое закрытие (эллиптическое ядро 5x5).
- Отображение вычисленного порога в интерфейсе.
- Предпросмотр исходного изображения и результата.
- Сохранение результата в файл (PNG/JPEG).

## Структура проекта

- `app.py` – главный класс приложения (GUI, вкладки, кнопки).
- `processor.py` – класс обработки изображений (загрузка, бинаризация, морфология, сохранение).
- `requirements.txt` – список необходимых библиотек.
- `screenshots/` – скриншоты работы программы.

## Установка и запуск

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/ваш-username/otsu_segmentation.git
   cd otsu_segmentation